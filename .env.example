# Copy this file to .env (gitignored) and fill in the real values.
# Scripts under scripts/ load these at runtime.

# Lab Windows 10 VM SSH password (used by scripts/d1_*.py)
MYDFIR_VM_PASSWORD=changeme

# (Future) other secrets the n8n workflow consumes belong in the n8n
# credential store, not here. See vault/runbooks/secrets-management.md.
