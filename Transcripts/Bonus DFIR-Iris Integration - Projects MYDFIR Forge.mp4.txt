 Now the next thing to do is integrate a ticketing system using Deferiris. If we go over to our virtual machine and click on our Deferiris virtual machine, type in IP space A, and actually I do have this in my notes, but it is here anyways. I'll open up another PowerShell window, type in SSH.

 mydefer at 192.168.136.140. Type in yes, and that right there is our virtual machine. What we'll want to do is git clone their GitHub repository, and I'll be sure to leave the link down below as well.

 After we're done, go ahead and change into that directory, which is iris-web. And we'll do a git checkout v2.4.22. And finally, let's copy the .env.model to our .env file. And this is just for demo purposes. Once that is done, let's do sudo docker-compose.

 And we'll type in pull. We get a docker-compose not found, and that is because we don't have that yet. So I'll do a sudo app install docker.io. And now that I think about it, we might get an error because it is going over to ir.archive.ubuntu.com. And actually, I'm not even going to wait. Play with the screen. Let's do a sudo nano etc apt sources.list.d. And we want to edit the ubuntu.sources.

 From here, let's go ahead and remove the .ir and hold Ctrl X, Y to save. Clear the screen. Let's do a sudo apt-get update and sudo apt-get upgrade, just in case. Once that is done, let's see if I can just sudo apt install docker-compose. Perfect.

 Once I have that ready to go, let's do a sudo docker-compose-pull, and we get a, hey, cannot extend service app, okay? Let's do a sudo nano, and what we wanna modify is the docker-compose-base, so this file right here. Compose.base.yaml. And what we're essentially looking for are the ones that say depends on. So this right here, let's go ahead and comment them out.

 just like so and i believe that is it cool ctrl x y and let's rerun the docker compost poll

 Once that is done, type in sudo docker-compose up. And eventually, we should be able to access our defer iris via HTTPS. But do note that your username and password is actually going to be displayed here. What that means is that you don't want to exit out your connection or clear the screen until you copy your username and password.

 If we scroll up, we could find a line that says info post underscore init. You can now log in with the user administrator and the password on port 443. Let me go ahead and copy that and let's paste that in here. That way we don't lose it.

 now in a web browser on my host machine i should be able to access our d for iris so 192.168.136.140 yes 140 right yeah 140. click on advanced proceed and there you go let's log in with our administrator account and we'll copy the password which is this right here and sign in

 Beautiful, we are good to go. Now we need to integrate this with our N8n workflow. To do that, you want to select the plus button, and then we'll type in dfir. We should see a defer-iris. For the credential, go ahead and select create new credential. The base URL, this is going to be our IP address of defer-iris.

 192.136.140 the api key this right here you can get it from our user account go into my settings and right here under api key let's go ahead and copy all of that now of course if you do this in production you want to create a different account you don't want to have it under your administrator account paste in our api make sure we ignore ssl because we don't have a valid certificate and save

 For the URL itself, what we wanna do here is take a look at its API documentation. If we go into help and under operations API,

 We can then click into the API version 2.0.4. And what we want to do is create an alert. So we want to add an alert right here. And the URI is forward slash alerts forward slash add. What that means is that we need to modify the URL to type in https 192.168.136.140 forward slash alerts.

 forward slash add and then here is the body schema so for example alert title we want to add that over here we want to check send body so the name is alert underscore title the value what we want to select the actual splunk alert but if we look on the left hand side all we see is slack we see send a message so how do we select splunk well let's click on back to canvas

 And rather than connecting Slack, what we'll do here is remove this branch and we'll connect ChatGPT over to D4Iris. And now if I click on webhook, I could see that my alert name is right here. So the alert title is going to be my search name field.

 just like so. And you can rinse and repeat and add all of these tags if you want. But from my testing, there are only a couple of fields that D for Iris requires you to have, which is the alert title, alert description, alert severity, status, and customer ID. So let me go ahead and add in the alert description. Click on add parameter.

 And for the description, I am going to output whatever ChatGPT created. So the message right here, the message content, I'm going to put that into my alert description. Because I think that's going to be pretty nice. Click on add parameter again. And this time we want the alert underscore severity underscore ID. And I am just going to put this as a random number. For example, three. Next is the alert status ID. And I'll put one here.

 And finally, the alert underscore customer underscore ID. And I'm going to say one. Now, if everything is good to go, let's try and execute the step

 and we get a consider using the ignore SSL issue. Okay, to do that, we can scroll all the way down and click on options and ignore SSL issues. Make sure that is checked. And now let's try executing the step. Method not allowed. Please check that you're using the right HTTP method. Scrolling up, my method right now is get. That is incorrect. What we want is post.

 Now if you didn't know, well if you go over to D4iris documentation, you can see the HTTP method on the left hand side. This right here is a POST method. So that is why we need to make sure that we are selecting POST. Beautiful. This looks like it worked. Let's go over to D4iris, and we'll click into the alerts. And there it is. How awesome is that? I don't know about you, but this is incredible. We now have a valid ticketing system that we can use in our SOC. That's pretty cool.

